{
  "name": "deploy_counts",
  "version": "1.0.0",
  "description": "Get deploy counts",
  "main": "getDeploys.js",
  "scripts": {
    "start": "node ./dist/index.js",
    "dev": "npx nodemon --inspect ./dist/index.js | bunyan",
    "build": "npx tsc -d --project ./tsconfig.json",
    "build:watch": "yarn run build -w",
    "test": "jest",
    "process": "node tasks/index.js"
  },
  "jest": {
    "setupFiles": [
      "./jest.setup.js"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.1",
    "jest": "^28.1.3",
    "nock": "^13.2.9",
    "nodemon": "^2.0.20",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "moment": "^2.29.4",
    "octokit": "^2.0.5"
  }
}
